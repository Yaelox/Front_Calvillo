import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RutasForaneasPage } from './rutas-foraneas.page';

describe('RutasForaneasPage', () => {
  let component: RutasForaneasPage;
  let fixture: ComponentFixture<RutasForaneasPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RutasForaneasPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
